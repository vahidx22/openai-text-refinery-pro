# OpenAI Text Refinery (Pro) — n8n node (v0.1.0)

This package provides an n8n node that performs multi-stage text editing with chunking, memory/tail propagation, and persistence (workflow static / Google Drive).

## Install (locally build .tgz)
1. Clone files into a folder `openai-text-refinery-pro`
2. Run:
